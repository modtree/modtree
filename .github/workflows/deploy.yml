name: auto deploy

on:
  push:
    branches:
      - main
      - dep/**

jobs:
  docs:
    if: |
      github.ref_name == 'dep/docs' ||
      github.ref_name == 'dep/all' ||
      github.ref_name == 'main'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: amondnet/vercel-action@v20 #deploy
        with:
          vercel-token: ${{ secrets.TURBO_TOKEN }} # Required
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }} #Required
          vercel-project-id: ${{ secrets.VERCEL_DOCS_PROJECT_ID }} #Required
          vercel-args: '--prod' #Optional

  frontend:
    needs: [docs]
    if: |
      always() &&
      github.ref_name == 'dep/front' ||
      github.ref_name == 'dep/app' ||
      github.ref_name == 'dep/all' ||
      github.ref_name == 'main'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: amondnet/vercel-action@v20 #deploy
        with:
          vercel-token: ${{ secrets.TURBO_TOKEN }} # Required
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }} #Required
          vercel-project-id: ${{ secrets.VERCEL_WEB_PROJECT_ID }} #Required
          vercel-args: '--prod' #Optional

  backend:
    if: |
      github.ref_name == 'dep/back' ||
      github.ref_name == 'dep/app' ||
      github.ref_name == 'dep/all' ||
      github.ref_name == 'main'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: akhileshns/heroku-deploy@v3.12.12
        with:
          heroku_api_key: ${{ secrets.HEROKU_API_KEY }}
          heroku_app_name: 'modtree-server' #Must be unique in Heroku
          heroku_email: 'brew4k@gmail.com'

  telegram:
    needs: [frontend, backend, docs]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: ./.github/actions/telegram
        with:
          telegram-token: ${{ secrets.TELEGRAM_TOKEN }}
          telegram-chat-id: ${{ secrets.TELEGRAM_GROUP }}
          needs: ${{ toJSON(needs) }}
