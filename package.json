{
  "name": "modtree-monorepo",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "apps/backend/functions",
    "packages/*"
  ],
  "scripts": {
    "deploy:backend": "turbo run deploy --filter=functions",
    "build": "turbo run build",
    "build:backend": "turbo run build --filter=functions",
    "build:deps": "turbo run build --filter=modtree",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "setup": "yarn && yarn build:deps && yarn"
  },
  "devDependencies": {
    "prettier": "latest",
    "turbo": "latest"
  },
  "engines": {
    "npm": ">=7.0.0",
    "node": ">=14.0.0"
  },
  "packageManager": "yarn@1.22.18"
}
